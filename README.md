# ChainForge

From raw blockchain data to structured onchain intelligence.

The public website lives in [`web/`](web/). Design source of truth is in [`demo/`](demo/).

```bash
cd web
npm install
npm run dev
```
